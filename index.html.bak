<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>shadow_xyz 🔥</title>
  <link rel="icon" href="https://github.com/Shadow-nex.png" type="image/png">
  <meta name="description" content="Shadow_xyz.">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: radial-gradient(circle at center, #000000, #020202, #050a10);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      height: 100vh;
      text-align: center;
      overflow: hidden;
      position: relative;
    }

    body::before {
      content: "";
      position: absolute;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(0,212,255,0.15) 1px, transparent 1px);
      background-size: 50px 50px;
      animation: moveParticles 20s linear infinite;
      z-index: 0;
      opacity: 0.4;
    }

    @keyframes moveParticles {
      from { transform: translate(0, 0) rotate(0deg); }
      to { transform: translate(-50px, -50px) rotate(360deg); }
    }

    h1 {
      font-size: 2.2rem;
      margin-bottom: 25px;
      color: #00d4ff;
      text-shadow: 0 0 15px #00d4ff;
      animation: glow 2s infinite alternate;
      z-index: 2;
    }

    @keyframes glow {
      from { text-shadow: 0 0 10px #00d4ff; }
      to { text-shadow: 0 0 25px #00d4ff, 0 0 50px #00d4ff; }
    }

    /* Imagen de perfil */
    .profile {
      position: relative;
      z-index: 2;
      margin-bottom: 10px;
    }

    .profile img {
      width: 130px;
      height: 130px;
      border-radius: 15px;
      object-fit: cover;
      box-shadow: 0 0 25px rgba(0,212,255,0.5);
      border: 2px solid rgba(0,212,255,0.4);
      transition: all 0.3s ease;
    }

    .profile img:hover {
      transform: scale(1.05);
      box-shadow: 0 0 35px rgba(0,212,255,0.8);
    }

    .profile-name {
      font-size: 1.2rem;
      margin-top: 8px;
      color: #00d4ff;
      text-shadow: 0 0 10px #00d4ff;
    }

    .buttons {
      display: flex;
      flex-direction: column;
      gap: 15px;
      width: 90%;
      max-width: 400px;
      z-index: 2;
    }

    .btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.2);
      color: #fff;
      font-size: 1rem;
      padding: 15px 20px;
      border-radius: 50px;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 0 10px rgba(0,212,255,0.1);
      backdrop-filter: blur(6px);
    }

    .btn i {
      font-size: 1.3rem;
    }

    .btn:hover {
      transform: scale(1.05);
      border-color: #00d4ff;
      box-shadow: 0 0 20px rgba(0,212,255,0.4);
    }

    /* Colores personalizados */
    .github { background: #181717; }
    .whatsapp { background: #128C7E; }
    .channel { background: #00aaff; }
    .channel2 { background: #005580; }

    footer {
      position: fixed;
      bottom: 10px;
      font-size: 0.9rem;
      color: #666;
      z-index: 2;
    }
  </style>
</head>
<body>

  <div class="profile">
    <img src="https://files.catbox.moe/fft2hr.jpg" alt="shadow_xyz">
    <div class="profile-name">shadow_xyz</div>
  </div>

  <h1>👋 Bienvenido a mi espacio</h1>

  <div class="buttons">
    <a href="https://github.com/Shadow-nex" target="_blank" class="btn github">
      <i class="fab fa-github"></i> GitHub
    </a>

    <a href="https://wa.me/51919199620" target="_blank" class="btn whatsapp">
      <i class="fab fa-whatsapp"></i> Contacto
    </a>

    <a href="https://whatsapp.com/channel/0029Vaj5mivLdQegrUZ1Xl3M" target="_blank" class="btn channel">
      <i class="fas fa-bullhorn"></i> Canal de WhatsApp
    </a>

    <a href="https://whatsapp.com/channel/0029VbBCHSsJZg41QE3Ibl3O" target="_blank" class="btn channel2">
      <i class="fas fa-bullhorn"></i> Canal Secundario
    </a>
  </div>

  <footer>© 2025 Shadow.xyz — Todos los derechos reservados.</footer>

</body>
</html>